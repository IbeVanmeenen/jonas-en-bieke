'use strict';

var webstarter = {};

/* ==========================================================================
   Webstarter - Component
   ========================================================================== */

webstarter.component = function () {

    var exports = webstarter.component;

    // Private function
    var privateFunction = function privateFunction() {};

    // Public function
    exports.publicFunction = function () {
        // Public function
    };

    // Init
    var init = function () {
        // Init (self-executing function)
    }();
};

/* ==========================================================================
   Webstarter
   ========================================================================== */

webstarter.app = function () {

    var exports = webstarter.app;

    // Scroll
    var appScroll = function appScroll() {
        // Put functions in the '_update' function :-)
        var latestKnownScrollY = 0,
            ticking = false;

        var _onScroll = function _onScroll() {
            latestKnownScrollY = window.pageYOffset;
            _requestTick();
        };

        var _requestTick = function _requestTick() {
            if (!ticking) {
                window.requestAnimationFrame(_update);
            }
            ticking = true;
        };

        var _update = function _update() {
            ticking = false;
            var currentScrollY = latestKnownScrollY;

            // webstarter.component.updateScroll(currentScrollY);
        };

        // scroll mousewheel wheel
        window.onscroll = function (e) {
            _onScroll();
        };
    };

    // On load
    exports.onload = function () {};

    // Init
    var init = function () {
        webstarter.component();
    }();
};

var ready = function ready(fn) {
    // Sanity check
    if (typeof fn !== 'function') return;

    // If document is already loaded, run method
    if (document.readyState === 'complete') {
        return fn();
    }

    // Otherwise, wait until document is loaded
    document.addEventListener('DOMContentLoaded', fn, false);
};

ready(function () {
    webstarter.app();
});

window.onload = function () {
    webstarter.app.onload();
};